import 'package:flutter/material.dart';

class CreatePoolScreen extends StatelessWidget {
  const CreatePoolScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: [
        Text('Create pool screen ')
      ]),
    );
  }
}
